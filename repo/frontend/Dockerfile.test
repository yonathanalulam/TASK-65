FROM node:18-alpine

WORKDIR /app

# Copy package files first for dependency caching
COPY package.json package-lock.json* ./

RUN npm ci

# Copy source
COPY . .

# Run tests
CMD ["npx", "vitest", "run"]
